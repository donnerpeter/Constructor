package cons2

/**
 * @author peter
 */
class Team {
  List<Frame> members

  def Team(Frame member) {
    this.members = [member]
  }

  Frame getAt(String role) {
    members.find { it.role == role }
  }
}
